# Dependencies
node_modules/
*/node_modules/

# Environment files
.env
*/.env
!.env.example
!*/.env.example

# Schwab OAuth tokens
.schwab-tokens.json
*/.schwab-tokens.json

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
.pnpm-debug.log*

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Build outputs
dist/
build/
*.tsbuildinfo

# OS files
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Testing
coverage/
.nyc_output

# PM2
.pm2/
pids/
logs/
prod-logs/

# Temporary files
*.tmp
.temp/

# Claude IDE settings
.claude/

# Deploy state
.last-deploy

# Runtime data directories
data/
*/data/

# Windows file attributes
*.Zone.Identifier
*:Zone.Identifier

# Binary/download files
*.deb
*.rpm
*.exe
*.msi

# Backtest sweep output
backtest-engine/research/output/sweeps/

# Per-research-dir output artifacts (CSVs, .txt logs, candidate JSONs).
# Regeneratable from the scripts in the same research dir; bloats git and
# routinely produces >100MB files that GitHub will reject. The scripts and
# SUMMARY.md stay tracked — only the output/ subdir is ignored. Files
# already tracked from older research dirs (e.g. first-hour, ls-overlay)
# stay tracked; this rule only affects new additions.
backtest-engine/research/*/output/

# Skunkworks projects
skunkworks/
# Greenfield: regenerable caches and binary artifacts (masters live in backtest-engine/data/)
backtest-engine/greenfield/**/__pycache__/
backtest-engine/greenfield/**/*.npz
backtest-engine/greenfield/**/*.pkl
backtest-engine/greenfield/explore/cache/
backtest-engine/greenfield/explore/cache_*
backtest-engine/greenfield/explore/C1-touches.csv.gz
backtest-engine/greenfield/explore/R1-oi-cache.csv
backtest-engine/greenfield/explore/episodes.csv

# [2026-08-23] Top-level research/ derived artifacts. These are REGENERABLE from the
# scripts in the same dir and were adding ~4.4GB to a single push (git choked at
# "Compressing objects 99%"). Masters live in backtest-engine/data/ or are re-derivable.
research/**/.venv/
research/**/__pycache__/
research/tick-engine/cache/
research/tick-engine/wicks/
research/tick-engine/reversal/
research/tick-engine/firstbreak/
research/tick-engine/touchgrid/
research/tick-engine/level-episodes/
research/tick-engine/mtf/
research/mbo-resting-orders/derived/
research/price-structures/analysis/
