# RESTART PLAN — rebuilding the book on solid data (2026-07-16)

**Context:** the LSTB adverse-cancel lookahead (KNOWABILITY.md) emptied the book —
no live strategies remain. Drew's directive: fix the engine (done), re-establish
backtest data as ground truth as-of its stamps (done — see KNOWABILITY.md), then
restart strategy work on the verified foundation. This is the plan.

## Phase 0 — Stop the bleeding (immediate, mostly done)

- [x] Engine knowability fixes (5) + ATM-IV relabel — 2026-07-15/16, uncommitted.
- [x] **Commit the fixes.** Done 2026-07-16 (Drew).
- [x] **Live: disable LSTB.** Done 2026-07-16 — Drew disabled ALL live trading.
- [x] Port as-of labeling (`Math.ceil`, per `precompute-short-dte-iv.js`) into
      `precompute-iv.js` so a future regen can't resurrect floor labels.
      Done 2026-07-16. Verification finding: cbbo-1m `ts_recv` is already
      interval-END snapped, so the 1m floor labels were already as-of — the +60s
      relabel was an over-shift; `qqq_atm_iv_1m.csv` restored to original labels
      (regen with fixed script = byte-identical). 15m relabel stands. Details in
      KNOWABILITY.md fix #6.

## Phase 1 — Re-baseline everything under the honest engine (1-2 sessions)

Regenerate golds for every strategy that might return, on the fixed engine + as-of
data. Expect numbers to DROP; the point is a trustworthy floor, not a pretty one.
Order by likelihood of surviving:

1. **GLF (gex-level-fade)** — its causal rerun already survived ($87,040 / PF 1.36 on
   nq-cbbo-causal); it uses its own −16min GEX lag and no LS inputs. Re-run with the
   fixed engine (ltLevels fix touches its liquidity filters, slippage already fixed).
2. **DWF v1/v1.1-B** — mechanism-grounded, placebo-controlled, no LS inputs; rerun,
   then the OOS data purchase (~$150-200 TCBBO extension) remains the gate.
3. **First-hour tracks T5/T7/T8** — research-validated with OOS; re-verify the
   research sims against the honest engine before any promotion.
4. **ISG (iv-skew-gex)** — rerun on causal GEX + relabeled as-of IV; it was
   "survives-thin" (PF 1.43) before; treat < PF 1.3 as dead.
5. **GLX** — remains ON HOLD; requires re-tune on causal GEX regardless, plus the
   live-LT parity gap (KNOWABILITY.md residual) must be resolved first.
6. **LGPR / v1-ES** — reruns on causal GEX + fixed engine; ES race data limits apply.

Deliverable: a new `STRATEGY-GOLD-STANDARDS.md` section headed "HONEST BASELINES
2026-07" — every number in it generated by the fixed engine, every input series
listed with its knowable-at semantics.

## Phase 2 — Verification harness (the lesson of this incident)

The lookahead survived three audit waves because all verification ran through the
same engine. Institutionalize independent cross-checks:

1. **TV parity harness** — `ereptor/strategies/LSTB Trigger Bar.pine` proved a
   one-day TV export catches what internal reruns can't. For every strategy that can
   be expressed in Pine, build the parity build, run quarterly, diff per-trade
   (join script: `research/vp-defended-wick/` TV-diff pattern). Budget: hours each.
2. **Independent-implementation spot check** — for any gold candidate, a standalone
   1s research sim (the `05-flip-analog.py` pattern) written WITHOUT reading the
   engine's execution code, matched on n/WR/PF within ~10% (existing CLAUDE.md rule
   #6 — now with teeth: the check must be a separate implementation, not the engine
   re-invoked).
3. **Knowability audit gate** — no new series OR loader-derived field enters the
   engine without a KNOWABILITY.md row (stamp meaning, knowable-at, live emission,
   evidence). Template in KNOWABILITY.md.
4. **Live-shadow reconciliation** — first 30 days of any new live strategy: nightly
   diff of live fills vs the engine's same-day backtest (the trade-audit tool
   exists); alert at >10% composition or WR divergence. The June "live
   underperformance = plumbing" investigations were this signal, unheeded.

## Phase 3 — GREENFIELD research (Drew's directive, 2026-07-16)

**If Phase 1 leaves no survivors, the restart is a TRUE reset.** Drew, verbatim
intent: all historical research is presumed tainted — it was run on bad data. The
new search must NOT be anchored in ANY prior research, candidate list, ranked
queue, "survivor", "dead end", or past assumption about where edge lives. Approach
the honest dataset as brand-new green-field data. Concretely:

- No prior research report, memory file, or REPORT.md is admissible as a starting
  point, prior, or pruning device for new hypotheses.
- Hypotheses are generated from the DATA and from market mechanism first
  principles, not from what "worked" or "failed" before — both labels came from a
  broken engine and are equally untrustworthy.
- The project's sole success criterion is a strategy that makes money on the
  honest dataset and live. Negative results have no standalone value.

What DOES carry forward is engineering, not research: the KNOWABILITY.md data
contract, the honest engine, the 1s-fill research rules (CLAUDE.md), the
independent-verification harness (Phase 2), and the raw datasets themselves.
Correctness infrastructure is not an "assumption" — it is the floor the reset
stands on. (This includes the standing knowability audit for any series entering
the engine; a data feed with unresolved stamp semantics enters as data to be
audited, with no inherited trust either way.)

## Phase 4 — Live re-entry criteria

A strategy goes live only when ALL hold:
1. Honest-engine gold, positive every year of its window, PF ≥ 1.3 book-quality or
   explicit diversifier role.
2. Independent-implementation cross-check within tolerance (Phase 2.2).
3. Knowability rows for every input (Phase 2.3).
4. 30-day live-shadow reconciliation clean (Phase 2.4).
5. Sizing from the honest DD distribution (account-growth-plan rebuild — the current
   frontend ladder references void LSTB numbers and must be regenerated).

---
*Squad note: dashboard/frontend surfaces still show LSTB as the recommended
small-account vehicle (`slingshot/frontend/src/utils/accountGrowthPlan.js`,
PlatformStatus copy). Update once Drew decides on live disable.*
